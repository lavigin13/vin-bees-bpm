// Environment configuration for standalone web mode:
// Auth via Basic Auth (login page), proxied API URL

/**
 * Get the API base URL.
 */
export const getApiBaseUrl = () => {
    return '/VinBeesERP/hs/API';
};
